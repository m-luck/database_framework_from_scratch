
from BTreeImplementation import BTreeImplementation

class Arrable:
    
    def __init__(self, file=None):
        indices = BTreeImplementation
        records = []
        
        if data_path:
            with open(data_path, "r") as f:
                for line in f:
                    key, val = line.split("|")
                    if key[0].isdigit():
                        truth[int(key)] = int(val)
                        db.insert(int(key), int(val))
        
    
